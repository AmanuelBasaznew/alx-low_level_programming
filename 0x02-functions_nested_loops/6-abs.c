#include "main.h"

/**
 * _abs(int) - finds the absolute value of the required number
 *
 * @n: is the number whose absolute is calculated
 */

int _abs(int)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
