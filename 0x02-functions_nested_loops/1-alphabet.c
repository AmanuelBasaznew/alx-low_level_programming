#include <main.h>

/**
 * main - Entry point
 *
 * Description: Function that prints out the alphabet
 *
 * Return: 0
 */

void print_alphabet(void)
{
	int ch;
	
	for (ch, ch <= z, ++ch)
		_putchar(ch);
	_putchar('\n');
}

