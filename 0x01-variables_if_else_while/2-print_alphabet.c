#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: Using putchar to print all lowercase letters of the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char alpha[] = "abcdefghijklmnopqrstuvwxyz";

	putchar(alpha);
	putchar('\n');
	return (0);
}
